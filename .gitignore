__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.joblib
# but DO ship the fitted UMAP reducer the live server reuses to place
# newly-inferred flows into the exported cloud's 2D space
!webui/public/data/*/umap.joblib
*.pt
data/processed/
checkpoints/
eval_results/
wandb/
.env
*.egg-info/
dist/
build/

# server-side live inference store (grows at runtime, not a source artifact)
server/data/

# Claude Code runtime/session artifacts (locks, etc.)
.claude/
